# Complete-fix plan

Preserve the approved layout and animations while closing the September 15 audit findings.

1. Save enquiries with delivery and review state; retry failed notifications; provide protected review actions and a local operations command.
2. Preserve each unavailable sitemap route family independently and test partial failures.
3. Align resource engagement analytics with the backend and add a contract regression check.
4. Generate public-route HTML metadata during builds, including case/article titles, descriptions, canonical URLs and images. Verify actual HTTP responses.
5. Complete newsletter confirmation and unsubscribe handling, with a documented consent-aware sending workflow.
6. Verify local build-tool invocation, document production configuration and recovery, and add checks for the changed behavior. npm is healthy; the earlier invocation failure came from sandbox access restrictions, not a broken installation.
7. Run regression checks, rebuild the preview, refresh the source manifest with an explicit change record, and report externally dependent work honestly.

Live mail delivery needs the owner's email provider credentials. Real production sign-in needs the final deployed origin and provider configuration. Relationship claims and verification dates need business confirmation; do not invent new verification dates.
